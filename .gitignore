node_modules/
dist/
out/
_site/
*.vsix
*.log
.DS_Store
.env

# Phase 9: screenshot pipeline intermediates. Raw frames are workbench-sized
# dev-host captures, regenerated by scripts/mac/shots.sh; the cropped gallery
# is derived output. What ships lives in the site repo (img/listing/auspexlens)
# after Diego's visual pass — neither belongs in history here.
docs/listing/raw/
build/
out-integration/
